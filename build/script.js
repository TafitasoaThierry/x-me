document.getElementById("year").innerHTML = (new Date().getFullYear() - 2023).toString();
/*
let kisaka:any = document.getElementsByClassName("kisaka")[0] as HTMLElement;
let mandeha:boolean = true;
let miverina:boolean = false;
let fetra:number = 300;
let fiatombohany:number = 0;
let x:number = fiatombohany;

setInterval(() => {
    console.log(x);
    if((mandeha) && (x != fetra)) {
        kisaka.scrollLeft = x;
        x = x + 1;
    }else if((mandeha) && (x == fetra)) {
        mandeha = false;
        miverina = true;
        kisaka.scrollLeft = x;
    }else if((miverina) && (x != fiatombohany)) {
        kisaka.scrollLeft = x;
        x = x - 1;
    }else {
        mandeha = true;
        miverina = false;
        kisaka.scrollLeft = x;
    }
}, 50)
*/
